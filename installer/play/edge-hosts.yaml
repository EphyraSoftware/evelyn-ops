---
all:
  hosts:
    nodea:
    nodeb:
  children:
    nfs:
      hosts:
        nodea:
      vars:
        storage_disks: /dev/sdb,/dev/sdc
    control:
      hosts:
        nodea:
      vars:
        node_hostname: nodea
        node_ip: 192.168.1.2
    compute:
      hosts:
        nodeb:
      vars:
        node_ip: 192.168.1.15
